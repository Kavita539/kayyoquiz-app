type LogInUserInputTypes = {
    email: string;
    password: string;
};

type UserDetailTypes = {
    firstName: string;
    lastName: string;
    confirmPassword: string;
    agreement: string;
};

export type {
    LogInUserInputTypes,
    UserDetailTypes
}; 