type ResultCardPropsTypes = {
    result: {
        question: string;
        options: string[];
        questionIndex: number;
        correctOption: number;
    };
};

export type {
    ResultCardPropsTypes
};